# Migration `20200901231610-init`

This migration has been generated by Thorsten Schaeff at 9/1/2020, 11:16:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Attribute" (
"id" SERIAL,"key" text  NOT NULL ,"value" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Price" (
"amount" integer  NOT NULL ,"currency" text  NOT NULL ,"id" SERIAL,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Sku" (
"id" SERIAL,"image" text  NOT NULL ,"inventoryCount" integer   ,"productId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Category" (
"id" SERIAL,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Product" (
"description" text   ,"id" SERIAL,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."_AttributeToSku" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "public"."_PriceToSku" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "public"."_CategoryToProduct" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE UNIQUE INDEX "Category.name" ON "public"."Category"("name")

CREATE UNIQUE INDEX "_AttributeToSku_AB_unique" ON "public"."_AttributeToSku"("A","B")

CREATE  INDEX "_AttributeToSku_B_index" ON "public"."_AttributeToSku"("B")

CREATE UNIQUE INDEX "_PriceToSku_AB_unique" ON "public"."_PriceToSku"("A","B")

CREATE  INDEX "_PriceToSku_B_index" ON "public"."_PriceToSku"("B")

CREATE UNIQUE INDEX "_CategoryToProduct_AB_unique" ON "public"."_CategoryToProduct"("A","B")

CREATE  INDEX "_CategoryToProduct_B_index" ON "public"."_CategoryToProduct"("B")

ALTER TABLE "public"."Sku" ADD FOREIGN KEY ("productId")REFERENCES "public"."Product"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_AttributeToSku" ADD FOREIGN KEY ("A")REFERENCES "public"."Attribute"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_AttributeToSku" ADD FOREIGN KEY ("B")REFERENCES "public"."Sku"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_PriceToSku" ADD FOREIGN KEY ("A")REFERENCES "public"."Price"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_PriceToSku" ADD FOREIGN KEY ("B")REFERENCES "public"."Sku"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_CategoryToProduct" ADD FOREIGN KEY ("A")REFERENCES "public"."Category"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_CategoryToProduct" ADD FOREIGN KEY ("B")REFERENCES "public"."Product"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200901231610-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,46 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Attribute {
+  id    Int    @default(autoincrement()) @id
+  key   String
+  value String
+  skus  Sku[]  @relation(references: [id])
+}
+
+model Price {
+  id       Int    @default(autoincrement()) @id
+  currency String
+  amount   Int
+  skus     Sku[]  @relation(references: [id])
+}
+
+model Sku {
+  id             Int         @default(autoincrement()) @id
+  attributes     Attribute[] @relation(references: [id])
+  image          String
+  inventoryCount Int?
+  prices         Price[]     @relation(references: [id])
+  product        Product     @relation(fields: [productId], references: [id])
+  productId      Int
+}
+
+model Category {
+  id       Int       @default(autoincrement()) @id
+  name     String    @unique
+  products Product[] @relation(references: [id])
+}
+
+model Product {
+  id          Int        @default(autoincrement()) @id
+  name        String
+  description String?
+  variants    Sku[]
+  categories  Category[] @relation(references: [id])
+}
```


